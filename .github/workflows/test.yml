name: Deployment My Storage
on:
  push:
    branches:
      - main
  workflow_dispatch:
jobs:
  my-frontend-deploy:
    environment: ${{github.ref_name == 'main' && 'PROD' || 'TEST'}}
    runs-on: ubuntu-latest
    env:
      USER: "Ubuntu"
    steps:
      - run: aws
