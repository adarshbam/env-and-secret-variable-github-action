name: My first workflow
on:
  push:
    branches:
      - main
      - develop
jobs:
  my-first-job:
    runs-on: ubuntu-latest
    env:
      USER: "Ubuntu"
    steps:
      - run: echo ${{secrets.AWS_SECRET}}
      - run: echo ${{vars.DIR_PATH}}
      - run: echo Hello
      - run: echo $USER
